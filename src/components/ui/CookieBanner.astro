<div id="cookie-banner">
  <div class="cookie-content">
    <div class="cookie-text">
      <div class="cookie-icon">🍪</div>
      <div class="cookie-message">
        <strong>Мы ценим вашу конфиденциальность</strong>
        <p>Этот сайт использует cookies для улучшения работы, анализа трафика и персонализации. 
          Продолжая использовать сайт, вы соглашаетесь с нашей 
          <a href="/file/cookie_v1.pdf" target="_blank" class="cookie-link">политикой использования cookies</a>.</p>
      </div>
    </div>
    <div class="cookie-actions">
      <button id="cookie-accept">Хорошо</button>
    </div>
  </div>
</div>

<style>
#cookie-banner {
  position: fixed;
  bottom: 20px;
  left: 50%;
  transform: translateX(-50%) translateY(100%);
  width: calc(100% - 40px);
  max-width: 660px;
  background: rgba(0, 9, 59, 0.98);
  backdrop-filter: blur(10px);
  color: #fff;
  padding: 20px 24px;
  border-radius: 20px;
  box-shadow: 0 20px 40px rgba(0,0,0,0.4);
  z-index: 9999;
  opacity: 0;
  transition: all 0.4s ease;
  pointer-events: none;
  border: 1px solid rgba(206, 206, 206, 0.507);
}

#cookie-banner.show {
  transform: translateX(-50%) translateY(0);
  opacity: 1;
  pointer-events: auto;
}

.cookie-content {
  display: flex;
  /* flex-direction: column; */
  align-items: center;
  gap: 16px;
}

.cookie-text {
  display: flex;
  gap: 12px;
  line-height: 1.5;
}

.cookie-icon {
  font-size: 32px;
  flex-shrink: 0;
  filter: drop-shadow(0 2px 4px rgba(0,0,0,0.2));
}

.cookie-message {
  flex: 1;
}

.cookie-message strong {
  display: block;
  font-size: 16px;
  margin-bottom: 6px;
  color: #fff;
  font-weight: 600;
}

.cookie-message p {
  font-size: 13px;
  color: #e0e0e0;
  margin: 0;
  line-height: 1.5;
}

.cookie-link {
  color: #70abdb;
  text-decoration: none;
  border-bottom: 1px solid rgba(41, 98, 219, 0.4);
  transition: all 0.3s ease;
  font-weight: 500;
}

.cookie-link:hover {
  color: #69a5d6;
  border-bottom-color: #69a5d6;
}

.cookie-actions {
  display: flex;
  justify-content: flex-end;
}

#cookie-accept {
  background: linear-gradient(135deg, #142ebd, #678ee2);
  border: none;
  color: white;
  padding: 10px 24px;
  border-radius: 12px;
  cursor: pointer;
  font-size: 14px;
  transition: all 0.3s ease;
  font-weight: 600;
  letter-spacing: 0.3px;
  box-shadow: 0 2px 8px rgba(76, 175, 80, 0.3);
}

#cookie-accept:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(76, 175, 80, 0.4);
  background: linear-gradient(135deg, #422bc5, #6b80dd);
}

#cookie-accept:active {
  transform: translateY(0);
}
/* ===== МОБИЛЬНАЯ ВЕРСИЯ (до 768px) ===== */
@media (max-width: 768px) {
  #cookie-banner {
    bottom: 16px;
    padding: 18px 20px;
    width: calc(100% - 32px);
    max-width: 100%;
    border-radius: 16px;
    left: 50%;
    transform: translateX(-50%) translateY(100%);
  }

  #cookie-banner.show {
    transform: translateX(-50%) translateY(0);
  }

  .cookie-content {
    flex-direction: column;
    gap: 14px;
    align-items: stretch;
  }

  .cookie-text {
    gap: 10px;
    flex-direction: row;
  }

  .cookie-icon {
    font-size: 28px;
  }

  .cookie-message strong {
    font-size: 14px;
    margin-bottom: 4px;
  }

  .cookie-message p {
    font-size: 12px;
    line-height: 1.6;
  }

  .cookie-link {
    font-size: 12px;
  }

  .cookie-actions {
    justify-content: stretch;
  }

  #cookie-accept {
    padding: 12px 20px;
    font-size: 14px;
    width: 100%;
    white-space: normal;
    border-radius: 10px;
  }
}

/* ===== МАЛЕНЬКИЕ ТЕЛЕФОНЫ (до 480px) ===== */
@media (max-width: 480px) {
  #cookie-banner {
    bottom: 10px;
    padding: 14px 16px;
    width: calc(100% - 20px);
    border-radius: 14px;
  }

  .cookie-content {
    gap: 12px;
  }

  .cookie-text {
    gap: 8px;
  }

  .cookie-icon {
    font-size: 24px;
  }

  .cookie-message strong {
    font-size: 13px;
    margin-bottom: 3px;
  }

  .cookie-message p {
    font-size: 11px;
    line-height: 1.5;
  }

  .cookie-link {
    font-size: 11px;
  }

  .cookie-actions {
    justify-content: stretch;
  }

  #cookie-accept {
    padding: 10px 16px;
    font-size: 13px;
    border-radius: 8px;
    min-height: 44px; /* Удобно для касания пальцем */
  }
}
</style>

<script>
(function() {
  'use strict';

  const COOKIE_KEY = 'cookie_consent_session';

  function hasAcceptedCookies() {
    return sessionStorage.getItem(COOKIE_KEY) === 'true';
  }

  function showBanner() {
    const banner = document.getElementById('cookie-banner');
    if (banner) {
      setTimeout(() => {
        banner.classList.add('show');
      }, 500);
    }
  }

  function hideBanner() {
    const banner = document.getElementById('cookie-banner');
    if (banner) {
      banner.classList.remove('show');
    }
  }

  function acceptCookies() {
    sessionStorage.setItem(COOKIE_KEY, 'true');
    hideBanner();
  }

  function initCookieBanner() {
    if (!hasAcceptedCookies()) {
      showBanner();
    } else {
      hideBanner();
    }
  }

  function setupAcceptButton() {
    const acceptButton = document.getElementById('cookie-accept');
    if (acceptButton) {
      acceptButton.addEventListener('click', acceptCookies);
    }
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', function() {
      initCookieBanner();
      setupAcceptButton();
    });
  } else {
    initCookieBanner();
    setupAcceptButton();
  }

})();
</script>