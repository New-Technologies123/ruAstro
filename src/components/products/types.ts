export interface ProductParam {
  label: string;
  value: string;
}

export interface Product {
  id: number;
  title: string;
  description: string;
  price: string;
  nds: string;  
  deliveryTime: string;
  image: string;
}
